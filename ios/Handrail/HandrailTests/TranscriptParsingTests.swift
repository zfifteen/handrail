import XCTest
@testable import Handrail

final class TranscriptParsingTests: XCTestCase {
    func testCurrentChatBlockParsingBehaviorIsPending() throws {
        throw XCTSkip("Phase Three iteration 7 will extract and test the shared transcript parser.")
    }
}
